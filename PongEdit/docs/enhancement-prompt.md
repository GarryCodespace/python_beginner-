# PongEdit Enhancement Prompt

Make PongEdit feel like a reliable video workbench. Keep one primary upload flow, show clear status/progress states, explain no-rally outcomes without clutter, expose only the highlight download, improve result readability, and prevent stale or confusing actions.

Execution requirements:

- Keep the first screen as the upload and processing workbench.
- Do not add a marketing landing page.
- Keep the copy honest: this is heuristic motion detection, not AI.
- Show the selected file name and size before processing.
- Show a compact processing timeline so the user knows what is happening.
- Show a clear no-rally result when no highlight can be generated.
- Keep only the highlight download action visible.
- Make the detected rallies table easier to scan.
- Preserve mobile responsiveness.
- Keep backend output URLs minimal and avoid advertising unused downloads.
